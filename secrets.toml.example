# Copy this file to .streamlit/secrets.toml and fill in your real key.
# .streamlit/secrets.toml is already gitignored (see .gitignore) --
# never commit the real file, never paste a real key into this template.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
